dist: bionic
language: cpp
compiler: gcc
sudo: required

script:
 - python3 -m pip install --upgrade pip
 - python3 -m pip install --upgrade pygount
 - pygount libs -f summary -s c
 - source ./build.sh
 - source ./runtests.sh
